---
- name: Run make command
  command: make
  args:
    chdir: "{{ ansible_env.HOME }}/inception"

- name: Start WordPress with Docker Compose
  command: make up
  args:
    chdir: "{{ ansible_env.HOME }}/inception"
  become_user: "{{ ansible_env.USER }}"
